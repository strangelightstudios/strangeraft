# strangeraft-memstore

This is an in-memory example `RaftLogStorage` and `RaftStateMachine` implementation based on [strangeraft](https://github.com/databendlabs/strangeraft/).

This crate is built mainly for testing or demonstrating purpose.:)
